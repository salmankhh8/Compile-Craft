import { DisableAutoFocusDirective } from './disable-auto-focus.directive';

describe('DisableAutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
